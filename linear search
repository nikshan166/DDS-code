#include <stdio.h>
using namespace std;
int main(){
    int arr[]={10,20,80,100};
    int n=sizeof(arr)/sizeof(arr[0]);
    int low=0;
    int index=-1;
    key=80;
    int high=n-1;
    while(low<=high){
        intmid=(low+high)/2;
        if(arr[mid]==key){
            index=mid;
            break;
        }
        else
        if(arr[mid]<key){
            low=mid+i;
            
        }
        high=mid-1;
    }
        }
    if(index==-1;
    cout<<"not found";
    }
    else{
        cout<<"found at index"<<index;
    }
    }
    
}
